#Loading the ggplot2 package:
library(ggplot2)

#Setting the parameters:
T <- 1        
N <- 1000     
dt <- T / N   

#Setting the time grid:
t <- seq(0, T, length.out = N + 1)

#Generating Brownian motion increments:
dW <- rnorm(N, mean = 0, sd = sqrt(dt))

#Constructing the Brownian path:
W <- c(0, cumsum(dW))

# Visualizing the motion:
ggplot() +
  geom_line(aes(x = t, y = W), color = "darkblue") +
  labs(title = "Simulated BM Path", x = "Time", y = "Position")+
  theme(plot.title = element_text(size = 12.5, face = "bold"))

